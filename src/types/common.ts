export interface Position {
  x: number;
  y: number;
}

export interface Size {
  width: number;
  height: number;
}

export type { Habit, HabitCompletion, JournalNote, HabitWithCompletions, CreateHabitInput } from '@/schemas/habit';
